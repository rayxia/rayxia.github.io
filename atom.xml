<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>老猎人工坊</title>
  
  <subtitle>Make my weapons strong and polish</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://rayxia.github.io/"/>
  <updated>2018-01-31T07:35:14.398Z</updated>
  <id>http://rayxia.github.io/</id>
  
  <author>
    <name>Ray</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>NSObject在初始化时做了什么</title>
    <link href="http://rayxia.github.io/2018/01/25/NSObject%E5%9C%A8%E5%88%9D%E5%A7%8B%E5%8C%96%E6%97%B6%E5%81%9A%E4%BA%86%E4%BB%80%E4%B9%88/"/>
    <id>http://rayxia.github.io/2018/01/25/NSObject在初始化时做了什么/</id>
    <published>2018-01-25T13:56:53.000Z</published>
    <updated>2018-01-31T07:35:14.398Z</updated>
    
    <content type="html"><![CDATA[<p>我们都知道，在OC中，对象的初始化分为两个阶段，一个是内存分配，也就是调用<code>alloc</code>方法，另一个是初始化，即调用<code>init</code>方法。那么这两个方法具体是在干什么呢。由于objc的runtime是开源的，所以我们可以直接从<a href="https://opensource.apple.com/tarballs/objc4" target="_blank" rel="noopener">源码</a>中窥探一二。</p><blockquote><p>oc是在不断更新的，所以会有多个版本，每个版本的实现会有所差别，我们用的版本是objc-706</p></blockquote><h2 id="一些重要的数据结构"><a href="#一些重要的数据结构" class="headerlink" title="一些重要的数据结构"></a>一些重要的数据结构</h2><p><strong>objc_object**</strong>和<strong>**objc_class</strong></p><p>OC的所有类都是基于一个根类，也就是NSObject，而NSObject的实质其实是一个结构体，它的很多方法其实都是在操作这个结构而已。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">@interface NSObject &lt;NSObject&gt; &#123;</span><br><span class="line">    Class isa  OBJC_ISA_AVAILABILITY;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面这段代码是从源码中直接抠出来的，我们可以看到，NSObject里面有一个实例变量isa，其类型为Class。那Class又是什么，继续追踪代码可以看到这一行<code>typedef struct objc_class *Class;</code>，其实很清晰了，Class的实质是一个指向object_class的一个指针。</p><p>下面我们来简单看看objc_class里面有什么。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">struct objc_class : objc_object &#123;</span><br><span class="line">//     Class ISA;</span><br><span class="line">    Class superclass;</span><br><span class="line">    cache_t cache;             // formerly cache pointer and vtable</span><br><span class="line">    class_data_bits_t bits;    // class_rw_t * plus custom rr/alloc flags</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过代码可以看到，object_class其实也不是根部的一个结构体，它还继承于objc_object这个结构，等会儿我们再来看这个东西。objc_class里面有四个数据结构(其中ISA被注释掉，其实这个变量以前继承于objc_object）。</p><blockquote><p>由于涉及到很多数据结构，所以本文并不会对每个数据结构进行详细的解释，只是大概的看一下他的作用,如果有兴趣可以把源码下载下来深入研究。</p></blockquote><p>第一个变量是superclass，其实一目了然，它其实是指向父类的一个指针；第二个cache_t则是一个用于缓存的结构体；第三个是一个class_data_bits_t结构体，它的作用则是存储有关于类的一些信息。</p><p>再来看看objc_object，这个类其实更简单了。内部只有一个私有变量<code>isa</code>，其余的方法大部分都是基于<code>isa</code>的操作以及对引用计数的管理。</p><p><strong>isa_t</strong></p><p>其实我们对于<code>isa</code>这个东西是不会太陌生的，这部分就来具体看看它到底有什么作用。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">union isa_t </span><br><span class="line">&#123;</span><br><span class="line">    isa_t() &#123; &#125;</span><br><span class="line">    isa_t(uintptr_t value) : bits(value) &#123; &#125;</span><br><span class="line"></span><br><span class="line">    Class cls;</span><br><span class="line">    uintptr_t bits;</span><br><span class="line"></span><br><span class="line">#   define ISA_MASK        0x00007ffffffffff8ULL</span><br><span class="line">#   define ISA_MAGIC_MASK  0x001f800000000001ULL</span><br><span class="line">#   define ISA_MAGIC_VALUE 0x001d800000000001ULL</span><br><span class="line">    struct &#123;</span><br><span class="line">        uintptr_t nonpointer        : 1; </span><br><span class="line">        uintptr_t has_assoc         : 1;</span><br><span class="line">        uintptr_t has_cxx_dtor      : 1;</span><br><span class="line">        uintptr_t shiftcls          : 44; // MACH_VM_MAX_ADDRESS 0x7fffffe00000</span><br><span class="line">        uintptr_t magic             : 6;</span><br><span class="line">        uintptr_t weakly_referenced : 1;</span><br><span class="line">        uintptr_t deallocating      : 1;</span><br><span class="line">        uintptr_t has_sidetable_rc  : 1;</span><br><span class="line">        uintptr_t extra_rc          : 8;</span><br><span class="line">#       define RC_ONE   (1ULL&lt;&lt;56)</span><br><span class="line">#       define RC_HALF  (1ULL&lt;&lt;7)</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><blockquote><p>为了便于阅读，上面的代码在原来的基础上减去了一些预编译的判断，只留下了在x86_64上的代码。</p></blockquote><p>isa_t是一个union，学过c的相信不会太陌生，union只能存储一个数据结构，并且union的大小由占用位数最大的那个变量决定，在这里，struct是最大的，一共64位。isa_t里面最重要的其实是<code>uintptr_t  bits</code>，它存储了有关isa_t的所有信息，也就是struct里面包含的那些信息。</p><p>简单的说明一下这些信息的意思</p><p>nonpointer: isa是否是非指针。由于oc的更新，现在的isa并不是一个指针</p><p>has_assoc: 是否有关联引用，也就是和associated object有关系</p><p>has_cxx_dtor: 是否有c++或者ARC析构函数。如果没有，变量可以释放的更快</p><p>shiftcls: 类指针的非零的位</p><p>maigc: 用于调试器从垃圾回收中分辨是否是一个真正的对象</p><p>weakly_referenced：引用方式是否为weak</p><p>deallocating：是否正在析构</p><p>has_sidetable_rc：retain count是否太大</p><p>extra_rc: 额外的ratain count计数位</p><p>通过这面的说明其实我们可以看到，isa_t其实存储了许多有关类的信息，是一个非常基础的数据结构。而正如我们下面即将谈到的，alloc所作的工作其实很多都是在对isa进行初始化</p><h2 id="alloc"><a href="#alloc" class="headerlink" title="alloc"></a>alloc</h2><p>当我们调用NSObject的alloc方法是，最终调用的是<code>callAlloc(Class cls, bool checkNil, bool allocWithZone=false)</code>这个方法，其内部代码有很多判断，因为根据class状态的不同可能对其分配内存的方式也不同，我们也不用太去纠结其分配内存的方式，所以我将<code>callAlloc</code>的代码进行了一些精简</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">static ALWAYS_INLINE id</span><br><span class="line">callAlloc(Class cls, bool checkNil, bool allocWithZone=false)</span><br><span class="line">&#123;</span><br><span class="line">    if (slowpath(checkNil &amp;&amp; !cls)) return nil;</span><br><span class="line"></span><br><span class="line">    bool dtor = cls-&gt;hasCxxDtor();</span><br><span class="line">    id obj = (id)calloc(1, cls-&gt;bits.fastInstanceSize());</span><br><span class="line">    if (slowpath(!obj)) return callBadAllocHandler(cls);</span><br><span class="line">    obj-&gt;initInstanceIsa(cls, dtor);</span><br><span class="line">    </span><br><span class="line">    return obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过对代码的精简可以看到，首先调用c函数<code>calloc</code>来分配一块内存并将所有的变量初始化为0或NULL。然后调用<code>initInstanceIsa</code>来对isa进行初始化,最后返回objc</p><p>顺便提一句，NSObject的init方法做了什么？其实什么也没做，其方法实现是空的。init方法主要让子类去继承，然后让子类去初始化自己的成员变量。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>其实类的初始化比我上面解释的复杂得多，正是由于其复杂性，本文也不会用太多的文字去解释每一个细节，这篇文章算是一个抛砖引玉吧，如果有兴趣，可以打开源码自己去一步一步的剖析</p><p><em>参考资料：</em></p><p><a href="http://www.sealiesoftware.com/blog/archive/2013/09/24/objc_explain_Non-pointer_isa.html" target="_blank" rel="noopener">Non-pointer isa</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;我们都知道，在OC中，对象的初始化分为两个阶段，一个是内存分配，也就是调用&lt;code&gt;alloc&lt;/code&gt;方法，另一个是初始化，即调用&lt;code&gt;init&lt;/code&gt;方法。那么这两个方法具体是在干什么呢。由于objc的runtime是开源的，所以我们可以直接从&lt;a h
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Swift对象的初始化</title>
    <link href="http://rayxia.github.io/2018/01/17/Swift%E5%AF%B9%E8%B1%A1%E7%9A%84%E5%88%9D%E5%A7%8B%E5%8C%96/"/>
    <id>http://rayxia.github.io/2018/01/17/Swift对象的初始化/</id>
    <published>2018-01-17T11:15:54.000Z</published>
    <updated>2018-01-18T02:58:13.122Z</updated>
    
    <content type="html"><![CDATA[<p>在swift中，对象的初始化和oc中类似，分为两个阶段，一个阶段是对象的内存分配，另一个阶段是对对象中的store value进行初始化。不过第一个阶段和oc略有不同，oc在第一阶段分配内存后，会将所有的属性设为0或者null，而swift则更为灵活，可以指定一些除0以外的值。</p><h2 id="全能初始化方法和便利初始化方法"><a href="#全能初始化方法和便利初始化方法" class="headerlink" title="全能初始化方法和便利初始化方法"></a>全能初始化方法和便利初始化方法</h2><p>在详细分析两段式(two-phrase)初始化之前，先了解一下swift中两种初始化方法：全能初始化方法(designated initializier)和便利初始化方法(convenience initializer)。</p><p>###全能初始化方法<br>就像其名字一样，通过全能初始化方法来初始化一个对象后，其所有的store property都能够进行初始化。一个类可以有多个全能初始化方法，但是一般来说只有一个。只要保证全能初始化方法能够初始化所有属性即可。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">class GameCharacter &#123;</span><br><span class="line">    var weapon: String</span><br><span class="line">    </span><br><span class="line">    init() &#123;</span><br><span class="line">        self.weapon = &quot;fist&quot;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    init(weapon: String) &#123;</span><br><span class="line">        self.weapon = weapon</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如上，Person类有两个全能初始化方法，一个<code>init()</code>，一个是<code>init(name: String)</code>，每个初始化方法都能够将name进行初始化。</p><blockquote><p>如果不在初始化方法中对非可选值的属性进行初始化，编译器会提示未初始化的错误。</p></blockquote><h2 id="便利初始化方法"><a href="#便利初始化方法" class="headerlink" title="便利初始化方法"></a>便利初始化方法</h2><p>便利初始化方法则只是初始化类中部分属性。看以下代码</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">class Warrior: GameCharacter &#123;</span><br><span class="line">    var shield: String</span><br><span class="line">    </span><br><span class="line">    init(weapon: String, shield: String) &#123;</span><br><span class="line">        self.shield = shield</span><br><span class="line">        super.init(weapon: weapon)</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    convenience init(shield: String) &#123;</span><br><span class="line">        self.init(weapon: &quot;fist&quot;, shield: shield)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Warrior有一个全能初始化方法<code>init(weapon: String, shield: String</code>，这个初始化方法可以初始化所有的属性，也就是<code>shield</code>和从父类继承来的<code>weapon</code>。另外还有一个便利初始化方法<code>init(shield: String)</code>，该方法只显示指定了shield的值，而weapon的值则是提供一个默认的值。我们注意到在这个初始化方法在前面加了一个关键字<code>convenience</code>。这个convenience除了表明这个方法是便利初始化方法外，还有一个作用就是让编译器进行一些错误检查。</p><p><strong>全能初始化方法和便利初始化方法之间的调用规则</strong></p><p>全能初始化方法和便利初始化方法调用规则有三个</p><ul><li>全能初始化方法必须调用父类的全能初始化方法</li><li>便利初始化方法只能调用当前类的初始化方法，不能调用父类的初始化方法</li><li>便利初始化方法，最终必须调用当前类的全能初始化方法。</li></ul><p>下图可以很直观的表明这三个规则：</p><p><img src="/2018/01/17/Swift对象的初始化/swift-01.png" alt="image"></p><h2 id="类的两段式初始化"><a href="#类的两段式初始化" class="headerlink" title="类的两段式初始化"></a>类的两段式初始化</h2><p>swift和oc最大的区别，除了多了很多新的特性，比如新的枚举类型，struct类型等，最明显的区别就是在编译器上面。swift的编译器在编译期间做了很多的安全检查工作，比如对于一个可能为nil的值，必须进行解包；两个不同类型的变量不能相互进行赋值（即使是Double和Int之间的也不能隐式转换）。类的初始化方法也一样。</p><p>在类的初始化方法中，有四个安全性的检查：</p><ul><li>全能初始化方法必须确保在父类被初始化之前，所有的属性被正确初始化</li><li>再给被继承的属性赋值之前，必须调用父类的全能初始化方法，也就是说，父类必须被初始化之后才能给被集成的属性赋值</li><li>便利初始化方法在对属性赋值之前，必须调用其它初始化方法</li><li>在当前类没有被正确初始化之前（即第一阶段），不能调用类的实例方法和读取实例属性</li></ul><p>下面这段代码说明了这四个检查</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">class Warrior: GameCharacter &#123;</span><br><span class="line">    var shield: String</span><br><span class="line">    </span><br><span class="line">    init(weapon: String, shield: String) &#123;</span><br><span class="line">        self.shield = shield // 在调用父类之前，必须对初始化本类的属性</span><br><span class="line">//        attack() 编译器错误，当前类还没初始化完成，不能调用实例方法</span><br><span class="line">        super.init(weapon: weapon)</span><br><span class="line">        self.weapon = &quot;sword&quot; //在父类初始化完成后，才能对从父类集成的属性进行赋值</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    convenience init(shield: String) &#123;</span><br><span class="line">        self.init(weapon: &quot;fist&quot;, shield: shield)</span><br><span class="line">        self.shield = &quot;silver shield&quot; //便利初始化方法在调用其它初始化方法后，才能对属性进行赋值</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    func attack() &#123;</span><br><span class="line">        Swift.print(&quot;use \(weapon) to attack&quot;)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果以上四个检查任何一个不符合，编译器都会报错。</p><p>而swift的两段式初始化正式基于这四个安全检查的，下面来看看这两个阶段分别作了什么</p><p><strong>第一阶段</strong></p><ul><li>调用全能初始化方法或者便利初始化方法，系统分配内存，但是并未对这个类进行初始化</li><li>在全能初始化方法中初始化所有属性</li><li>调用父类的全能初始化方法执行前两个相同的步骤</li><li>沿着继承链不断向上一直到根类为止</li><li>这个时候在继承链上的类都被正确的初始化，第一阶段完成</li></ul><p><strong>第二阶段</strong></p><ul><li>从根类开始向下返回，每个类的全能初始化方法都能够进行更多的对类的初始化操作，包括可以使用属性，可以调用实例方法</li><li>最后，任何便利方法都有机会去对类进行更多的初始化</li></ul><p>沿用上一个代码片段，当调用<code>init(weapon: String, shield: String)</code>时，第一阶段的初始化就开始了，直到GameCharacter的全能初始化方法中的<code>self.weapon = &quot;fist&quot;</code>，这时候第一阶段结束。然后再沿着继承链向下，直到Warrior类的全能初始化方法中的<code>self.weapon = &quot;sword</code>时，第二阶段结束。</p><p>我们可以看到，swift的二段式初始化能够保证我们在使用一个类的时候能够被正确的初始化，而且这个过程都是在编译时检查的，这样就能够确保我们在编写类的初始化代码的时候不会犯错。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;在swift中，对象的初始化和oc中类似，分为两个阶段，一个阶段是对象的内存分配，另一个阶段是对对象中的store value进行初始化。不过第一个阶段和oc略有不同，oc在第一阶段分配内存后，会将所有的属性设为0或者null，而swift则更为灵活，可以指定一些除0以外的
      
    
    </summary>
    
    
  </entry>
  
</feed>
